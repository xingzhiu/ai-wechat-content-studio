# 架构说明

```text
RSS / API
   │
   ▼
n8n 定时采集 ──► FastAPI ──► PostgreSQL
                         │
                         ├─ OpenAI 结构化评分与写稿
                         ├─ Pillow 品牌封面合成
                         ├─ ZIP 素材包导出
                         └─ 微信草稿适配器（默认 mock）
                                  │
React 审核后台 ◄──────────────────┘
```

## 关键约束

- URL 规范化和数据库唯一约束共同保证采集幂等。
- 标题 token Jaccard 相似度用于初步事件聚类；阈值为 0.55。
- 每日选题按得分降序，分数至少 7，排除 `needs_review`，最多选 3 个不同主题。
- 文章每次保存前自动建立版本快照。
- 导出和公众号草稿请求均要求文章先被人工批准。
- 公众号请求使用 `Idempotency-Key` 避免重复创建。

